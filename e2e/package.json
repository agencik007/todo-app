{
  "name": "todo-app-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright end-to-end tests for the full stack (see docker/docker-compose.e2e.yml)",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report",
    "typecheck": "tsc -p ."
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/node": "^22.20.4",
    "typescript": "~5.9.2"
  }
}
